"""Helix: the framework code for Self-Improving Agents.

Not a package. Just organized modules. Import via `from helix.X import Y` after
the repo root is on sys.path (handled by conftest.py for tests and by a small
sys.path shim in each chapter script).

Spec version: 0.1 (see SPEC.md).
"""

__spec_version__ = "0.1"
